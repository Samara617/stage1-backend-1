# Local example (adjust host/port/db/user/pass to your setup)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/string_analyzer?schema=public"


# Railway: paste the full connection URL from the Postgres plugin
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/railway?sslmode=require"


PORT=8080
NODE_ENV=development